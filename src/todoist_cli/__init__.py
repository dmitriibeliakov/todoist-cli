"""todoist_cli — personal Todoist CLI core.

The ``commands`` and ``client`` modules form the stable, reusable core that
the v1.1 MCP server will import directly. ``cli`` and ``formatting`` are
human/shell-surface only and must NOT be imported from ``commands``.
"""

__version__ = "1.0.0"
